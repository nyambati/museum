const http = require('http');
const app = require('../server');

const PORT = process.env.PORT || 3000;

const server = http.createServer(app);

server.listen(PORT);
server.once('listening', (_) => console.log(`Server running on port ${PORT}`));
